# ==========================================================
# 🌍 Redirecionamento principal (InfinityFree compatível)
# ==========================================================
<IfModule mod_rewrite.c>
    RewriteEngine On

    # 🚫 Evita loop quando já está dentro de /Visacont/public
    RewriteCond %{REQUEST_URI} !^/Imperio/public/

    # ✅ Redireciona tudo para Visacont/public
    RewriteRule ^(.*)$ Imperio/public/$1 [L]
</IfModule>

# ==========================================================
# 🚫 Protege arquivos sensíveis
# ==========================================================
<FilesMatch "\.(env|ini|log|json|lock|md|git|htaccess)$">
    Require all denied
</FilesMatch>
